import React from "react";
import PropTypes from "prop-types";

const SingleExperience = (props) => {
  return <div>Single experience</div>;
};

SingleExperience.propTypes = {};

export default SingleExperience;
